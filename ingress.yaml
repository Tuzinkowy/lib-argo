apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: argo
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
  - http: 
      paths:
      - path: /
        pathType: Prefix
        backend:
          service: 
            name: argo-cd-argocd-server
            port: 
              number: 80