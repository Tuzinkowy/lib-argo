apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: demo
spec:
  dnsNames:
    - ironcow.ddns.net
  secretName: example-app-tls
  issuerRef:
    name: letsencrypt-cluster-issuer
    kind: ClusterIssuer